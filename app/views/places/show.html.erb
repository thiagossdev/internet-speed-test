<p style="color: green"><%= notice %></p>

<%= render @place %>

<div>
  <%= link_to "Edit this place", edit_place_path(@place.external_id) %> |
  <%= link_to "Back to places", places_path %>

  <%= button_to "Destroy this place", place_path(@place.external_id), method: :delete  %>
</div>
